/**
 * Stub for multi-line ternary detection.
 * Used by operators.js. Minimal implementation so modules can load.
 */

export function detectMultiLineTernaries(_lineWithoutComments, _index, _lines, _hasQuestionMarkOutsideString) {
  return { isMultiLineTernaryConditionLine: false };
}
